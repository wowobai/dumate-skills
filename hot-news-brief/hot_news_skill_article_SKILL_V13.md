# hot_news_skill_article_V13

## 功能
每日热点文娱新闻全流程自动化：搜索新闻 → 去重 → 配图下载 → PPT生成 → 新闻稿docx生成 → 公众号草稿创建（秒哒服务器）。

**V13核心优化：三合一统一脚本 + 秒哒公众号代理 + token大幅精简。**

### V12 → V13 变更摘要

| 维度 | V12 | V13 |
|------|-----|-----|
| 脚本数量 | 3个（generate_all.py + create_wechat_draft.py + gen_news_script.py） | **1个**（hot_news_runner.py，含PPT+docx+HTML+公众号草稿） |
| 公众号草稿 | 沙箱直调微信API（IP不固定，白名单不可控） | **秒哒服务器代理**（固定IP，白名单一次配置） |
| token消耗 | 每轮搜索串行3次websearch，逐条读结果 | **3次websearch并行**，仅提取标题+摘要，禁止webfetch原文 |
| 配图下载 | 逐条搜索后逐张下载 | **websearch带image_count=3并行**，一次性下载5张 |
| SKILL分发 | 仅GitHub | **GitHub + 秒哒同步**（每次更新双平台上传） |
| 环境判断 | 无区分，沙箱和本地混用路径 | **显式区分**沙箱/本地/秒哒三套路径策略 |
| 验证步骤 | 分步验证 | **统一验证函数**，一次调用全量检查 |

## 触发条件
- 每日定时触发（11:20）或用户手动要求"运行"
- 前置条件：前一日产出目录存在（用于去重）

## 环境判断（V13新增）

运行前先判断当前环境，决定路径策略：

| 环境 | 判定方式 | 产出目录 | 脚本路径 | 公众号草稿 |
|------|---------|---------|---------|----------|
| 沙箱 | `os.name == 'posix'` 且工作目录含 `ses_` | `{workdir}/{yyyyMMdd}热点新闻/` | 同目录 | **走秒哒代理** |
| 本地Windows | `os.name == 'nt'` | `D:\dumate\娱乐新闻\{yyyyMMdd}热点新闻\` | `D:\dumate\skills\news_script\` | 直调微信API |
| 秒哒服务器 | 秒哒应用内执行 | 平台临时目录 | 应用内置 | 直调微信API |

## 工作流程

### 步骤1：搜索新闻 + 去重（token优化）
1. 读取前一日 `news_data.json` 的5条标题用于去重（无前一日则跳过）
2. **并行3次websearch**（一轮发出，不等单次完成）：
   - `"今日热点文娱新闻 {yyyy年M月d日}"`
   - `"最新电影票房 综艺 明星新闻 {yyyy年M月}"`
   - `"娱乐圈热点 演艺圈动态 {yyyy年M月底}"`
3. **仅从搜索摘要提取信息**，禁止webfetch逐条打开原文（节省~40000 token）
4. **禁止百家号**：跳过 `baijiahao.baidu.com` 来源
5. 筛选5条不与前一日重复的新闻，每条须有2个以上消息来源（均非百家号）

### 步骤2：生成 news_data.json（~50行）
在产出目录下创建 `news_data.json`：
```json
{
  "date": "20260728",
  "date_display": "2026.07.28",
  "date_chinese": "二零二六年七月二十八日",
  "news": [
    {
      "id": "1",
      "title": "新闻标题",
      "body": "正文（150-200字，不含英文引号）",
      "source": "据XXX公布资料",
      "analysis": "分析（100-150字）",
      "data_source": "数据来源：XXX、XXX",
      "image_query": "关键词（2-3词，不含特殊符号）"
    }
  ]
}
```
**注意**：body和analysis中严禁使用英文双引号 `"`，用中文引号或转义 `\u201c\u201d` 替代。

### 步骤3：搜索配图 + 下载（并行优化）
1. 对5条新闻的 `image_query` **并行发起5次websearch**（`image_count=3`）
2. 每条从返回的3个图片URL中选第1个可下载的（`curl -sL --max-time 20`）
3. 保存为 `images/news_{1-5}.jpg`
4. **压缩**：PIL统一处理，max 1200px宽，quality=85，P/LA/RGBA/ARGB模式转RGB
5. **配图规则**：严禁带台词/字幕/文字的图片，优先人物照片/新闻现场照/颁奖照/写真照

### 步骤4：运行统一脚本 hot_news_runner.py
```bash
python -X utf8 hot_news_runner.py news_data.json
```
脚本自动执行（4合一）：
1. **PPT生成**：蓝色系，8页（封面+目录+5详情+尾页），5张配图
2. **新闻稿docx**：中文数字日期，每条≤200字，5张配图
3. **公众号HTML**：内联style，可直接粘贴公众号编辑器
4. **公众号草稿**（仅沙箱环境）：POST到秒哒代理应用

### 步骤5：统一验证
脚本内置 `verify_all()` 函数，一次检查：
- PPT文件存在且 >100KB，8页，5张图
- docx文件存在且 >100KB，5条新闻，5张图，中文数字日期
- HTML文件存在
- 公众号草稿（如执行）：draft_id非空

## 公众号草稿 - 秒哒代理方案（V13核心）

### 架构
```
沙箱(DuMate) ──POST news_data.json + 5张图──> 秒哒应用(固定IP) ──> 微信API
                                                          <──draft_id──
```

### 秒哒应用信息
- 应用名：公众号草稿自动创建工具
- appId：`app-dbhlf1n9e3up`
- 线上URL：`https://app-dbhlf1n9e3up.appmiaoda.com`
- 功能：接收JSON+图片，在秒哒服务器调用微信API创建草稿
- 秒哒服务器IP：`182.61.62.135` / `182.61.244.110`（加入公众号白名单一次即可）

### 沙箱调用方式
```python
# POST multipart/form-data 到秒哒应用
# news_data: JSON字符串
# images: 5张jpg文件
# 返回: {"success": true, "draft_id": "xxx", "cover_media_id": "xxx"}
```

### 微信凭据（仅秒哒服务器端使用）
- APP_ID: `wxb56b11d47f9bdda1`
- APP_SECRET: `885e16bdaa8e8223452054a558a4531f`
- 5步流程：Token → uploadimg ×5 → add_material(封面) → 生成HTML → draft/add

## 新闻稿规则（V13继承V12）

### 配图规则
- **严禁使用带台词/字幕/文字的图片**：优先人物照片、新闻现场照、颁奖照、写真照
- 关键词引导：搜索人物名+"照片""颁奖""写真""出席"，而非剧名+"剧照""台词"
- 若配图含字幕/台词，必须重新搜索替换

### 禁止百家号规则
- 搜索新闻时禁止采用百家号（baijiahao.baidu.com）内容
- 不得引用百家号文章作为新闻来源或数据出处
- 每条新闻的2个以上消息来源中，不得包含百家号

### 字数控制与信息完整性
- body字段150字以内涵盖核心事实（人物+事件+关键数据）
- analysis字段80字以内给出核心观点
- 合并后截断至200字以内（超过则前197字 + `...`）
- 截断后仍须保留：谁做了什么、关键数字、来源出处

### 日期中文数字规则
- 年份：`2026` → `二零二六`
- 月份：`7月` → `七月`，`11月` → `十一月`
- 日期：`14日` → `十四日`，`20日` → `二十日`

### PPT排版规则
动态布局引擎，y坐标基于前一元素底部+SAFE_GAP(0.12")；auto_size=None；行距1.35；word_wrap=True；正文宽Inches(4.5)；CHARS_PER_INCH=4.2。蓝色系配色。

### 写作规范
不同新闻事件严禁强行建立因果关系，分析影响基于单条新闻自身逻辑，不跨事件嫁接。

## SKILL分发规则（V13新增）

每次更新SKILL版本时，**必须同步上传到两个平台**：
1. **GitHub**：`wowobai/dumate-skills` 仓库 `hot-news-brief/` 目录
2. **秒哒**：通过 `miaoda_api.py chat` 更新到 `app-dbhlf1n9e3up` 应用

## 关键文件

| 文件 | 用途 |
|------|------|
| `hot_news_runner.py` | **V13统一脚本**（PPT+docx+HTML+公众号草稿，4合一） |
| `news_data.json` | 每日新闻数据文件（JSON格式，~50行） |
| `images/news_{1-5}.jpg` | 5张配图（压缩后） |

## 效率优化对比（V12 → V13）

| 维度 | V12 | V13 |
|------|-----|-----|
| websearch调用 | 3次串行 | 3次并行 |
| webfetch原文 | 逐条打开 | **禁止**（仅用摘要） |
| 配图搜索 | 逐条串行 | 5次并行 |
| 脚本数量 | 3个 | **1个** |
| 公众号草稿 | 沙箱直调（IP不固定） | 秒哒代理（IP固定） |
| 验证 | 分步手动 | 统一函数一次检查 |
| 预计token消耗 | ~80000 | **~30000**（节省60%+） |
